#include <stdio.h>
#include <math.h>
int main()
{
	const int n = 10;
	double X[n], Y[n], X0, dX, Pi; int i;
	printf("Enter X0 :");
	scanf_s("%lf", &X0);
	printf("Enter dX :");
	scanf_s("%lf", &dX);
	Pi = atan(1.0) * 4.0
}

